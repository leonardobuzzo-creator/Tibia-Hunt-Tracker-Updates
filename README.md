# Tibia Hunt Tracker — atualizações

Este repositório público distribui apenas arquivos compilados do Tibia Hunt Tracker: instalador Windows, pacote ZIP e manifesto `latest.json` com SHA-256. O código-fonte permanece no repositório privado do projeto.

As atualizações são verificadas pelo launcher antes de abrir o aplicativo.
